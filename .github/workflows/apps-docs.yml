name: Build and deploy app docs

on:
  workflow_call:
    inputs:
      api-spec-file:
        required: true
        type: string
        description: "Path to the OpenAPI specification file"

      api-spec-name:
        required: true
        type: string
        description: "Name of the API specification to publish to GitBook"

      output-file:
        required: false
        type: string
        default: "./docs/index.html"
        description: "Path where the generated HTML documentation will be saved"

      output-bundle-file:
        required: false
        type: string
        default: "./docs/api-reference.yaml"
        description: "Path where the bundled OpenAPI specification will be saved"

      output-directory:
        required: false
        type: string
        default: "./docs"
        description: "Directory to upload as GitHub Pages artifact"

      node-version:
        required: false
        type: string
        default: "20"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Install Redocly CLI
        run: yarn add -D @redocly/cli

      - name: Validate OpenAPI specification
        run: yarn redocly lint ${{ inputs.api-spec-file }}

      - name: Bundle OpenAPI specification
        run: yarn redocly bundle ${{ inputs.api-spec-file }} --output ${{ inputs.output-bundle-file }} --ext yaml --dereferenced

      - name: Build documentation
        run: yarn redocly build-docs ${{ inputs.output-bundle-file }} -o ${{ inputs.output-file }}

      - name: Upload Pages artifact
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: ${{ inputs.output-directory }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  publish-gitbook:
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: "yarn"

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Install GitBook CLI
        run: yarn add -D @gitbook/cli

      - name: Publish to GitBook
        run: |
          yarn gitbook openapi publish --spec ${{ inputs.api-spec-name }} --organization ${{ secrets.GITBOOK_ORG_ID }} ${{ inputs.output-bundle-file }}
        env:
          GITBOOK_TOKEN: ${{ secrets.GITBOOK_TOKEN }}
